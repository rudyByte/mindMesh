{
  "name": "mindmesh-monorepo",
  "version": "1.0.0",
  "description": "MindMesh monorepo for Next.js web application and FastAPI backend.",
  "scripts": {
    "dev": "npx concurrently -n \"web,api\" -c \"cyan,magenta\" \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev --prefix web",
    "dev:api": "python -m uvicorn server.main:app --reload",
    "install:web": "npm install --prefix web",
    "install:api": "pip install -r server/requirements.txt",
    "install:all": "npm run install:web && npm run install:api"
  }
}
